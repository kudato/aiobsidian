{
  "name": "aio",
  "version": "0.1.0",
  "description": "The Obsidian half of aiobsidian: a local socket server inside the vault.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.8.0"
  }
}
